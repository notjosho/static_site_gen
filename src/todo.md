bugs xd

1. -fix the quotes

2. fix the lists, getting cut off, not
   applying the types correctly
   3.1 links
   - 3.2 unordered lists
   - 3.3 ordered lists

3. new lines are not being handled on code
   blocks

4. # fix this so it returns an html_node and a .to_html()
   html_content = markdown_to_html_node(from_path_str)


blockquote for all text
br if empty lines or /n